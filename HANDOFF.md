# 田园日志 — 下一会话接手说明

更新时间：2026-08-24（Asia/Dubai）

## 一句话状态

当前开发态已经完成页头与主菜单整体吸顶、中文化、WP 费用与不足提示、玩法说明入口、页面文案精简，以及设置页存档概况。本轮按用户要求只执行轻量 UI 主路径回归和 Chrome 页面实测，没有重复运行长跑、六策略或正式证据冻结。上一份正式冻结报告因此只保留为历史基线；当前结论是：

- `UI MAIN PATH = PASS`
- `FULL AUTOMATION FOR CURRENT CANDIDATE = NOT RERUN`
- `OVERALL RELEASE = HOLD`

当前没有未关闭的已知 S0、S1 或 S2。不要在三项人工门槛闭环前宣布“达到最终准出”。

## 产品形态

《田园日志》是纯单机、纯文字、按现实日期推进的农场与牧场养成游戏：

- 没有后端、账号、云同步或联机服务。
- 存档保存在当前浏览器的 LocalStorage。
- 设置页支持导出 / 导入单文件备份。
- 当前公开存档版本为 `save_version: 1`，采用双槽 + 提交指针、导入 / 迁移前备份和 12 个恢复点。
- v0→v1 仅为内部预发布迁移夹具，不代表曾发布 v0。

## 最近完成的用户需求

### 1. 页头与主菜单持续可见及切页定位

已完成：

- 标题、右上状态、玩法说明入口和主菜单组成一个整体吸顶区；页面下滑时不会拆开或滚走。
- 切页后主内容定位到整块吸顶区下方，不再跳脱或被页头遮挡。
- 窄屏导航保持单行并在导航自身内部横向滚动；当前页自动进入可见区。
- G、L、Esc、周摘要与菜单点击统一使用页面起点聚焦逻辑。
- 同页命令重绘仍保留阅读位置和稳定焦点，不会被强制卷回顶部。
- 搜索框、键盘焦点轮廓、单一 live region、重复播报、对话框说明关联及焦点恢复均已有回归覆盖。

主要实现：

- `src/presentation/styles.css`
- `src/presentation/app.js`
- `tests/ui-static.test.mjs`

### 2. 所有玩家提示与状态改为中文

已完成，截图中的三处现已显示为：

- `PLOTS` → `种地`
- `crop.irrigate_batch 已执行并保存。` → `批量灌溉完成了，已经保存。`
- `growing` → `生长中`

中文展示覆盖：

- 10/10 页面区段名。
- 37/37 语义命令。
- 作物、动物生命阶段、疾病、动物效果字段、加工、订单、工程、仓储异常、日志类型等 9 组状态 / 记录域，共 47 个内部值。
- 旧存档里的历史命令日志：按结构化 `command_type` 重新生成中文摘要，不直接显示旧英文命令 ID。
- 无效时区、损坏 JSON、物品不足、损坏导入和只读恢复等用户错误。
- 未知内部值使用中文兜底，不回显原始枚举或字段名。

有意保留的标准符号：`IANA`、`Asia/Shanghai` 等时区标识，`G` / `WP` 游戏单位，`G` / `L` / `Esc` / `Enter` 实体键名，以及稳定存档编号。

中文化只发生在展示边界。命令类型、状态枚举、内容 ID、存档字段、LocalStorage 键和黄金回放数据没有重命名，因此旧存档兼容不受影响。

主要实现：

- `src/presentation/labels.js`：集中中文映射与中文兜底。
- `src/presentation/app.js`：页面、操作结果、状态、旧日志与用户错误的中文展示。
- `index.html`：页头改为“田园日志 / 本机存档”。
- `src/core/new-save.js`、`src/core/clock.js`、`src/persistence/store.js`、`src/rules/inventory.js`：常见错误不再泄漏英文异常或物品 ID。
- `tests/localization.test.mjs`：5 个中文化专项回归。

真实 Chrome 已在最终 `dist/` 上打开已有存档并逐一检查十个页面。页面英文键、37 类命令前缀和已知英文状态枚举的可见文本扫描均为 0 命中；无效时区只显示中文提示，没有 `RangeError` 或原始无效输入。

### 3. “田区 / 待办”菜单切换不再跳动

2026-08-24 用户报告从较短“待办”页与较长“田区”页相互切换时，导航会在页头下方与吸顶位置之间上下跳动；传统非覆盖式滚动条平台还存在横向居中位移风险。

已完成：

- 页头仍可见时，点击菜单保持当前页头与导航位置，不再强制吸顶。
- 导航已经吸顶时，切页后继续保持吸顶，并把新正文定位在导航下方。
- 短页获得稳定的最小正文高度，可维持吸顶位置。
- 纵向滚动条槽固定，避免长短页切换造成横向布局位移。
- Chrome 最终构建量化复验：顶部状态两页均为 `navTop=135.4375 / scrollY=0 / navLeft=158`；吸顶状态两页均为 `navTop=0 / scrollY=190 / mainTop=74.8203125 / navLeft=158`。

主要实现：

- `src/presentation/app.js`
- `src/presentation/styles.css`
- `tests/ui-static.test.mjs`

### 4. 所有消耗 WP 的操作在按钮上明显提示

2026-08-24 用户要求所有需要消耗 WP 的操作在操作本身明显提示费用。

已完成：

- 15 类可能消耗 WP 的玩家命令全部纳入显式白名单：播种、收获、单块 / 批量灌溉、除草、施肥、圈舍清洁、放牧、动物互动 / 治疗、加工排队、工程投入、探索、居民交谈和自定义工作。
- 费用直接显示在触发按钮内部，采用边框、加粗、下划线标签；同一文本同时进入按钮无障碍名称。
- 固定费用、表单动态费用、当日共享费用和条件免重复收费均按真实规则显示；不会把已安排的重复操作误报成再次扣费。
- 自定义工作、工程投入和事件选择会随 WP / 专注输入或实际效果即时更新费用。
- Chrome 最终构建逐页复验“今日、田区、动物、加工、小镇”；自定义工作从 1 WP / 1 专注改为 3 WP / 2 专注时，可见文本和无障碍名称均同步更新。
- 静态测试从实现源码提取 WP 命令集合并与 15 类精确比较，防止以后新增命令漏标。

主要实现：

- `src/presentation/app.js`
- `src/presentation/styles.css`
- `tests/ui-static.test.mjs`

### 5. 右上角问号入口与玩家玩法说明

2026-08-24 用户要求在右上角增加问号图标，点击跳转到玩法说明文档。

已完成：

- 页头牧场状态区上方新增圆形 `?` 入口，实际视觉位置为页面最右上角。
- 链接可访问名称为“打开玩法说明（新窗口）”，具备悬停标题和明显键盘焦点轮廓。
- 点击后保留游戏页并在新标签打开 `help.html`，不会中断当前页面或读取 / 修改本机存档。
- 玩家说明覆盖快速开始、WP / 专注 / 资金、十个页面、日期推进与离线保护、键盘操作、存档与备份，并提供目录和返回游戏入口。
- 构建脚本把 `help.html` 收入正式 `dist/`，并在构建时校验问号链接目标存在。
- Chrome 最终构建已实际点击入口，确认新标签标题、URL、六章内容、返回入口及 HTTP 字节哈希。

主要实现：

- `index.html`
- `help.html`
- `src/presentation/styles.css`
- `scripts/build.mjs`
- `tests/ui-static.test.mjs`

### 6. 文案口语化与轻量主路径回归

2026-08-24 用户要求页面文案和操作尽量简单易懂、口语化，并明确要求 UI 小改不再重复全量回归，只维护一套主路径。

已完成：

- 十个页面的说明句均改成一句短话，例如“先看看情况，再安排今天的活”“要处理的事都放在这里”。
- 关键操作改成直接动词：`更新到今天`、`需要时灌溉`、`陪它玩`、`开始加工`、`接单`、`聊一聊`、`投入工时`、`导出备份`。
- 删除或改写面向玩家的开发 / 规则术语，例如“结算哈希”“渠道倍率”“确认排产”“稳定身份编号”“双槽提交指针”。关键费用、期限和风险确认仍保留。
- `help.html` 同步改成口语化说明。
- 新增 `npm run test:ui-main`，只运行 `tests/ui-main-path.test.mjs` 的一条主路径：十页文案、关键操作、WP 标签和玩法说明。
- 本轮轻量验证：语法检查 PASS、UI 主路径 1/1 PASS、构建 PASS、Chrome 十页菜单和玩法说明实走 PASS。
- 本轮没有运行内容校验、数值校验、全量 Node 测试、长跑、六策略或证据生成；不要把上一版正式哈希写成当前 `dist/` 的哈希。

主要实现：

- `src/presentation/app.js`
- `src/presentation/labels.js`
- `help.html`
- `tests/ui-main-path.test.mjs`
- `package.json`

### 7. 页头与主菜单整体固定

2026-08-24 用户要求页面下滑时，把截图中的标题、状态、问号入口和主菜单整块固定。

已完成：

- `site-header` 和 `main-nav` 统一包入 `site-shell`，由外层整体吸顶，菜单不再单独吸顶。
- 从下滑位置切换页面时，新正文按整块页头的实际高度定位；在页面顶部点击菜单不会额外卷动。
- 轻量验证：语法检查 PASS、UI 主路径 1/1 PASS、构建 PASS。
- Chrome 最终构建实测：滚到“今日”页底部时整块仍完整固定；直接切到“待办”后整块位置保持不变，正文从固定区下方开始。

主要实现：

- `index.html`
- `src/presentation/styles.css`
- `src/presentation/app.js`
- `tests/ui-main-path.test.mjs`
- `tests/ui-static.test.mjs`

### 8. 设置页显示完整存档概况

2026-08-24 用户提出存档信息应包含当前进度和具体保存时间，而不是只显示一串编号。

已完成：

- “新存档”卡片改为“当前存档”，依次显示当前游戏日、最后保存时间、资金 / 已开放田区 / 动物、进行中的作物 / 加工 / 建设、建档时间和存档编号。
- 存档编号降为次要信息；创建新存档前增加“会替换现在的进度，建议先导出备份”的直白提示。
- `SaveStore` 的 `written_at` 改为成功写入时的真实时间，并随 `save()` / `load()` 返回；不再把 `last_trusted_time` 冒充为保存时间。
- 时间按存档锁定时区显示到秒，并明确显示时区，例如 `2026-08-24 22:44:13（Asia/Dubai）`。
- 单一 UI 主路径加入保存时间写入 / 读取行为断言；本轮语法检查 PASS、UI 主路径 1/1 PASS、构建 PASS，没有运行全量持久化回归。
- Chrome 最终构建实测：真实设置页信息和语义结构正常；刷新前后保存时间从 `22:43:57` 更新到 `22:44:13`。

主要实现：

- `src/persistence/store.js`
- `src/presentation/app.js`
- `src/presentation/styles.css`
- `tests/ui-main-path.test.mjs`

### 9. 导出与导入备份样式统一

2026-08-24 用户要求统一备份区的导出和导入样式。

已完成：

- “导出备份”和“导入备份”统一为同尺寸、同边框、同一行的普通按钮。
- 隐藏浏览器原生“选择文件 / 未选择任何文件”界面；点击中文“导入备份”按钮仍会打开文件选择器。
- 原生文件输入同时退出无障碍树，避免读屏额外播报英文 `Choose File`；导入变更处理逻辑保持不变。
- 语法检查 PASS、UI 主路径 1/1 PASS、构建 PASS；Chrome 设置页视觉和无障碍结构实测 PASS，未运行全量回归。

主要实现：

- `src/presentation/app.js`
- `src/presentation/styles.css`
- `tests/ui-main-path.test.mjs`

### 10. 工时不足提示始终可见

2026-08-24 用户反馈页面滚动后执行操作，工时不足的结果提示藏在页面顶部看不到；截图中 `12/12 WP` 时相关按钮仍看起来可执行。

已完成：

- 所有带 WP / 专注费用的固定与动态操作，在渲染和输入变化时统一计算剩余量。
- 工时不足时按钮直接禁用，使用红色警示样式，并把费用标签改成“工时不足 · 还差 N WP”；专注不足同样显示具体缺口。
- 当日已安排、无需重复扣 WP 的操作保持可用，不会被余额检查误禁用。
- 其他操作失败或成功消息改为固定显示在整体吸顶区下方；页面停在下方时也能立刻看见。可见消息不新增 `role=status/alert`，继续只由原有单一 live region 播报，避免读屏重复。
- 补充 `randomToken()`：本地 HTTP 浏览器缺少 `crypto.randomUUID()` 时，建档和操作编号使用兼容生成方式；存档格式和编号用途不变。
- 轻量验证：语法检查 PASS、UI 主路径 1/1 PASS、构建 PASS。隔离端口真实建档后以页面操作用满 `12/12 WP`，田区“播种 12 格”显示“工时不足 · 还差 1 WP”并禁用；在长页面下方触发失败时固定消息可见。临时 `4174` 服务已关闭，正式 `4173` 保持运行。

主要实现：

- `src/presentation/app.js`
- `src/presentation/styles.css`
- `tests/ui-main-path.test.mjs`

### 11. WP 费用标签视觉简化

2026-08-24 用户反馈操作按钮里再套一层描边费用框，显得笨重且按钮过高。

已完成：

- WP 操作统一为单层按钮布局，操作名与费用在同一行；费用改成无描边的小圆角标签，不再使用块级、下划线和框中框样式。
- 常规费用文案从“消耗 1 WP”缩短为“需 1 WP”；重复安排提示缩短为“已安排 · 不再扣 WP”。
- 主操作按钮内的费用标签使用轻量半透明样式；工时不足仍保留红色警示标签和禁用状态。
- 桌面真实田区页面检查通过：灌溉、除草、施肥、收获四种按钮保持紧凑、对齐且费用可读。
- 语法检查 PASS、UI 主路径 1/1 PASS、构建 PASS，未运行全量回归。

主要实现：

- `src/presentation/app.js`
- `src/presentation/styles.css`
- `tests/ui-main-path.test.mjs`
- `tests/ui-static.test.mjs`（仅同步未来全量回归断言，本轮未执行）

### 12. 肥力满时不再施肥或扣资源

2026-08-24 用户指出肥力已经达到 `100` 时仍可“买肥料并施肥”，会无意义消耗 WP 和资金。

已完成：

- 肥力 `100` 时，操作按钮改为非交互状态“肥力已满，无需施肥”，不再给玩家一个无效操作入口。
- 规则层增加兜底：即使旧页面或重复命令提交 `crop.fertilize`，也不扣 1 WP、不扣 60 G、不消耗堆肥，肥力保持 `100`。
- 未满肥力时原施肥规则不变，仍正常显示和消耗 `1 WP`。
- 定向规则测试 1/1 PASS：满肥力前后资金 `2400 G`、已用工时 `0 WP`、肥力 `100` 均保持不变。
- UI 主路径 1/1 PASS、构建 PASS；真实 `4173` 存档中 A田区肥力 `100` 时只显示状态文字，不再出现施肥按钮。未运行全量回归。

主要实现：

- `src/core/engine.js`
- `src/presentation/app.js`
- `src/presentation/styles.css`
- `tests/core.test.mjs`
- `tests/ui-main-path.test.mjs`

### 13. 未开垦田区可直达对应工程

2026-08-24 用户询问 B田区的“开垦B田区”工程具体在哪里，并要求增加可点击跳转。

已完成：

- B田区说明改为“尚未开垦。去小镇开垦 →”，其中“去小镇开垦”是可点击、可键盘访问的真实链接。
- 点击后切换到“小镇”，直接滚动到“建设”区的“开垦B田区”工程卡片，不停在小镇页顶部。
- 目标工程卡片获得程序焦点和明显轮廓；价格 `1,200 G`、总需 `6 WP`、当前进度及“开始建设”立即可见。
- UI 主路径 1/1 PASS、构建 PASS；真实 `4173` 页面点击跳转、定位和焦点实测 PASS，未运行全量回归。

主要实现：

- `src/presentation/app.js`
- `src/presentation/styles.css`
- `tests/ui-main-path.test.mjs`

## 上一份正式冻结结果（当前文案小改后仅作历史）

玩法说明入口版曾按 `npm run verify` 的同一六阶段执行完整验证；以下结果绑定旧 `bundle_hash` `42533e…8bb4`，不代表当前口语化 `dist/`：

- 退出码：`0`
- Node 自动化：`115/115 PASS`
- 天气长跑：10,000 季，0 违规
- 事件导演：10,000 日，0 违规
- 属性 / 语义命令：100,000 样本、100,000 条命令、1,000 次幂等检查，0 违规
- 六策略：6 类 × 3 种子 × 84 日，共 18 局，0 违规
- 加工完成：200 批
- 购畜：6 次，其中大型动物 3 次、鸭 3 次；五物种均覆盖
- 畜棚完成：3 次
- 周订单生成：216 周；接受 / 交付 25 / 25；后半程来源交付 15 次
- 正常策略平均工时利用率：50%—76.19%
- `free_loop_detected=false`
- `dominant_strategy=null`
- 构建产物：31 个文件
- manifest 与实际 `dist/`：31/31 路径、字节、SHA-256 匹配
- 根入口、玩法说明、README、全部 `src/` 与 `dist/` 对应副本：31/31 字节一致

内容规模：19 作物、10 天气、5 动物、8 配方、8 建筑、8 居民、3 区域、183 事件；367 个事件选项 ID 全局唯一。

黄金回放：历史 v1 原样保留；当前机械化内容使用 v2，7 / 21 / 84 日状态与日志哈希均命中。

存档：v1 双槽、迁移 / 导入前原始备份、内部 v0→v1、未知及关闭模块字节等价保留、7 日 + 4 周 + 1 年初共 12 个恢复点、真实 5 MiB 五年容量验收均通过。

## 上一份正式冻结哈希（当前 `dist/` 已不再匹配）

- `bundle_hash`：`42533e1ab7fa9d5f068f98dd0ccb8d28d5c5e2eb7ca9ef14300e3496ca298bb4`
- `evidence_hash`：`e5a9f94aa0b27db68c455ff4e52b198573619a9d158ddd2002b8084b0918ced7`
- `evidence/release-manifest.json` SHA-256：`6c76a8d7e1021f6dce5137234439a828966dd86b04203aaf240bb7f058350bbe`
- `evidence/acceptance-summary.json` SHA-256：`7871ee84e1a98f743d6c8ea453c0de96bf2ffe781fb29f56c48f73e25bf7a347`
- `evidence/TC-052-053-browser-preflight-HELP-2026-08-24.md` SHA-256：`f741086754be03eacc2a36016cd138ad0465c00d3697004ead93df835d910ee3`
- `evidence/FINAL-QA-RESULT-HELP-2026-08-24.md` SHA-256：`7f17632948be3b480c2e80f9c366d3b26c5e99766afe2e9894337b5909d1239e`
- `evidence/DESIGN-REVIEW-FINAL-HELP-2026-08-24.md` SHA-256：`3110dda79c5488bfade9e26163528a3d3d0157518def5dcb340f2d4cd34629cb`
- TC-052 / 053 协议 SHA-256：`6cca25e0fac041a5944b058a116075e6a7f4a63428f77747d03bfa0ee0e87776`
- TC-055 协议 SHA-256：`6a8e4c8b2bb2e28b9ca6ec08c2b23049e05b8b4ace5acb6530ef13ed856d3798`

## 上一份正式报告与主要入口（历史冻结）

- `README.md`
- `package.json`
- `dist/index.html`
- `dist/help.html`
- `evidence/acceptance-summary.json`
- `evidence/release-manifest.json`
- `evidence/TC-MAPPING.md`
- `evidence/FINAL-QA-RESULT-HELP-2026-08-24.md`
- `evidence/DESIGN-REVIEW-FINAL-HELP-2026-08-24.md`
- `evidence/TC-052-053-browser-preflight-HELP-2026-08-24.md`
- `evidence/TC-052-053-manual-protocol.md`
- `evidence/TC-052-053-results-template.md`
- `evidence/TC-055-usability-protocol.md`
- `evidence/TC-055-results-template.md`
- `fixtures/golden-replays-v2.json`

## 尚未闭环的硬门槛

### TC-052 — 真人键盘验收

状态：`NOT RUN`。

真人须在同一冻结 `dist/` 上按照 `evidence/TC-052-053-manual-protocol.md` 完成 K-01—K-17，全程键盘操作，并填写 `evidence/TC-052-053-results-template.md` 与规定签署。

### TC-053 — 真人浏览器 + 辅助技术验收

状态：`NOT RUN`。

真人须使用协议规定的 Safari + VoiceOver，或 Chrome / Firefox + NVDA，完成 A-01—A-17 并签署。DOM 检查、自动化浏览器和 AI 代理不能替代真人听读。

### TC-055 — 真实 20 人新手研究

状态：`NOT RUN`。

必须按 `evidence/TC-055-usability-protocol.md` 招募 20 名合格真实首次用户，无玩法提示地完成播种、工作调整和自产产品出售到账；复合成功至少 16/20，并取得 QA 与 Game Designer 双签。当前结果模板 P01—P20 为空，没有伪造数据。

## 下一会话建议顺序

1. 日常页面文案、样式或静态说明小改：只运行 `npm run test:ui-main`、`npm run build`，再用浏览器走一次与改动直接相关的主路径；不要重复长跑和正式证据生成。
2. 玩法规则、存档、时间、经济、命令语义或内容发生变化时，运行受影响的专项测试；准备正式发布或真人 TC-052 / 053 前，再统一执行完整 `npm run verify` 和正式冻结。
3. 启动当前 `dist/`：

   ```sh
   cd /Users/edison/VS_Projects/farm-journal/dist
   node ../scripts/serve.mjs
   ```

   默认 URL 为 `http://127.0.0.1:4173/`。截至本文件写入时，该端口正在服务当前口语化开发候选；它尚未重新生成正式 manifest。

4. 真人执行前必须先为当前口语化候选完成全量验证、生成新 `bundle_hash`，再记录协议哈希、浏览器 / 操作系统完整版本、视口、时区、刷新点和配置来源。
5. TC-052 / 053 使用重新冻结后的 `dist/`，不要使用当前旧 manifest、仓库根目录、缓存副本或 `file://`。
6. 完成人工签署后，再组织 TC-055 真实 20 人研究；三项均通过且签署齐全后，才可把整体 RELEASE 从 HOLD 改为 PASS。

如果下一会话只是继续改页面文案 / 样式：沿用轻量主路径，不要每次重做完整冻结。如果准备发布、开始真人验收，或改动扩展到玩法 / 存档 / 内容，再一次性重新运行完整验证、核对 manifest 和哈希，并重写最终 QA / Design Review。

## 仓库与操作边界

- 原始 `docs/` 从开发开始至今未修改。
- 仓库没有提交，`HEAD` 不存在；项目文件仍全部为 untracked。
- 没有执行 Git commit 或 push。
- 不要删除或覆盖历史黄金基线 `fixtures/golden-replays-v1.json`。
- 不要把浏览器自动化预检写成 TC-052 / 053 的真人 PASS。
- 不要把模拟策略、AI、DOM 扫描或空白模板写成 TC-055 的真实受试结果。

## 历史重要缺陷状态

以下此前发现的问题均已修复并有自动化覆盖：

- `work.remove` 退还 WP / 专注却保留已执行播种或社交结果：`CLOSED`。
- 至少 168 / 183 事件分支结构性单边支配：`CLOSED`，内容校验含双向严格支配检查和退化反例。
- TC-059 只买鸡、不测畜棚 / 大动物且冻结 `real_date_key`：`CLOSED`，现覆盖五物种、畜棚生命周期、84 日逐周订单。
- TC-052 数字键直接执行、隐藏输入被编号：`CLOSED`，数字键只聚焦可见操作，Enter 才执行，高风险模态暂停快捷键。
- TC-053 应用内“减少动态”只保存不生效：`CLOSED`。
- live region 重复、事件预览播报不足、对话框说明关联和重渲染焦点丢失：`CLOSED`。
- 长页主导航离开视口、切页焦点跳脱 / 被遮挡：`CLOSED`。
- “田区 / 待办”长短页面互切时导航上下或横向跳动：`CLOSED`，顶部与吸顶两种状态均有真实 Chrome 坐标回归。
- 页面下滑时标题、状态和玩法说明入口会滚走，只留下主菜单：`CLOSED`，现由 `site-shell` 整块吸顶，并实测下滑后切页不拆开。
- 页面、提示、状态和旧日志泄漏内部英文键：`CLOSED`。
- 消耗 WP 的操作按钮没有明显显示实际费用：`CLOSED`，15 类命令精确覆盖，固定 / 动态 / 条件 / 共享费用均有回归与最终 Chrome 复验。
- 缺少随时可达的玩家玩法说明入口：`CLOSED`，页头右上角问号已链接到正式构建内的无脚本玩家指南。
- 页面说明和操作名称偏正式、技术味较重：`CLOSED`，十页主路径与玩法说明已口语化，并建立单一轻量 UI 回归。
- 设置页只显示存档编号，且旧 `written_at` 不一定代表真实保存时刻：`CLOSED`，现显示完整进度概况，并以成功写入时间为准。
- 备份区导入操作使用浏览器原生文件控件，与导出按钮样式不一致且读屏出现英文：`CLOSED`，现统一为两个等宽中文按钮。
- `12/12 WP` 时操作仍像可执行，点击后的工时不足消息又被吸顶区遮住：`CLOSED`，现前置显示具体缺口并禁用，其他结果消息固定可见。
- 田区肥力 `100` 时仍可施肥并浪费 WP / 资金：`CLOSED`，现不显示操作按钮，规则层也保证零消耗。
- B田区只写“通过开垦工程解锁”，无法知道入口位置：`CLOSED`，现可直接跳到小镇的对应工程卡片。
- 湿度、肥力、杂草只有裸数字，玩家无法判断好坏：`CLOSED`，现常驻显示口语状态和带安全区的 0—100 刻度；悬停、点击或键盘聚焦可查看与真实规则一致的阈值说明，且不依赖颜色单独传意。
- “今天的安排”允许手填任务并占用 WP / 专注，却不会执行实际经营操作：`CLOSED`，现改为只读“今日工时”，自动列出已完成操作和剩余资源；旧存档中尚未确认的手动预留仍可取消，避免资源被锁住。
- “今日”只显示天气名称和明日预报，无法判断具体影响：`CLOSED`，今天与明日天气均可点击展开影响和应对建议；10 种天气已全部写入玩家指南，并提供直达链接。

同日不带后缀的导航稳定性报告绑定 `581ef0…26009`，`WP-COST` 报告绑定 `b60c2c…7e8e2`，`HELP` 报告绑定 `42533e…8bb4`；它们均只保留为历史证据。当前口语化候选尚未生成新的正式报告与哈希。
